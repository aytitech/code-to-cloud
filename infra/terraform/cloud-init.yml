#cloud-config

packages:
  - curl
  - ufw

runcmd:
  # TODO: install Docker
  # TODO: create the stackshop user, add to sudo and docker groups
  # TODO: allow stackshop passwordless sudo
  # TODO: copy SSH keys to the stackshop user
  # TODO: enable UFW, allow 22, 80, 443
  # TODO: enable and start Docker
  # TODO: create the "proxy" docker network
